/* eslint-disable no-undef */
import Custom from '../../src/utils/custom'

describe('all Custom method testing', () => {
  it('created at function ', () => {
    expect(Custom.createdAt()).toBeDefined()
  })
})
